import React,{ useState,useEffect } from 'react';
import { useNavigate } from 'react-router-dom';

import {
  Button,
  Col,
  Row,
} from 'reactstrap';

import ComponentCard from '../../../components/ComponentCard';

const LabTests = () => {
  const navigate = useNavigate();
  const [data, setData] = useState([
    { id: 1, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Ambient  at 10°C'},
    { id: 2, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance'},
    { id: 3, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance1'},
    { id: 4, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance2'},
    { id: 5, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance3'},
    { id: 6, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance4'},
    { id: 7, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance5'},
    { id: 8, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance6'},
    { id: 9, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance7'},
    { id: 10, TestName: 'Delta E', Method: 'ASTM',TestDirection: 'Bally flex resistance8'},
  ]);
  // const data = [
  //   { id: 1, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 2, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 3, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 4, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 5, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 6, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 7, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 8, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 9, TestName: 'Delta E', Method: 'ASTM'},
  //   { id: 10, TestName: 'Delta E', Method: 'ASTM'},
  // ];
  const tableStyle = {
    // margin: 'auto', 
    // width: '60%',  
    // maxWidth: '1000px',
  };
  const handleEditClick = (item) => {
    // Navigate to the edit page with the item's id
    // Navigate(`/resources/address-types/edit/${itemId}`);
    navigate('/resources/lab-tests/edit', { state: item });
  };
  const handleEditAdd = () => {
    // Navigate to the edit page with the item's id
    // Navigate(`/resources/address-types/edit/${itemId}`);
    navigate('/resources/lab-tests/add');
  };
  const handleDeleteClick = async (itemId) => {
    try {
      // Call your API endpoint to delete the item
      // const response = await fetch(`your-api-endpoint/${itemId}`, {
      //   method: 'DELETE',
      //   headers: {
      //     // Your headers here (if needed)
      //   }
      // });
  
      // Check if the request was successful
      // if (!response.ok) {
      //   throw new Error(`Error: ${response.statusText}`);
      // }
  
      // Filter out the deleted item from your data state
      const updatedData = data.filter((item) => item.id !== itemId);
      setData(updatedData);
  
      console.log('Item deleted successfully');
    } catch (error) {
      //only checks for error that are generated by fetch function , and cors 
      console.error('Failed to delete the item', error);
    }
  };
  

  useEffect(() => {
    
    // Fetch the data from the API
    const fetchData = async () => {
      const token = localStorage.getItem('userToken');
      console.log('token',token);
      const response = await fetch('https://indiapuleather.com/teamasia/api/public/cities', {
        method: 'GET', 
        headers: {
          'Authorization': `Bearer ${token}`
        }
      });
      console.log('result',response);
      if (!response.ok) {
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const result = await response.json();
      setData(result); 
    };
  
    fetchData();
  }, []);

  return (
    <ComponentCard
    title=""
    subtitle={
      <p>
        {/* Overview of the projects */}
      </p>
    }
  >
    <Row>
      <Col md="8">
        <Button className='my-btn-color' style={{ marginBottom: '1rem',marginRight:'10px' }} onClick={() => handleEditAdd()}>
          Add Lab Test
        </Button>
      </Col>
    </Row>
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%' }}>
      <table className="table" style={tableStyle}>
              <thead>
              <tr>
            <th>Test Name</th>
            <th>Method</th>
            <th>Actions</th>
          </tr>
              </thead>
              <tbody>
                {data.map((product) => (
                  <tr key={product.id}>
                  <td>{product.TestName}</td>
                  <td>{product.Method}</td>
                  <td>
                    {/* Action buttons or icons */}
                      <button type="button" className="btn mybtncustomer btn-secondary btn-sm mr-2" onClick={() => handleEditClick(product)}><i className="bi bi-pencil-fill my-pen-color" /></button>
                      <button type="button" className="btn mybtncustomer btn-secondary btn-sm mr-2" onClick={() => handleDeleteClick(product.id)}><i className="bi bi-trash-fill my-trash-color" /></button>
                  </td>
                </tr>
                ))}
              </tbody>
            </table>
            
    </div>
   
  </ComponentCard>

   
   
  );
};

export default LabTests;